# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 语言

始终使用中文与用户交流。所有对话、解释、说明和代码注释都必须使用中文。

## 全局协作规则

### 1. 文件保存地址

需求开发过程中，直接或间接产生的所有文件，都需要询问用户保存地址。

流程：
1. 先推荐一个你认为最合适的地址（附上推荐原因）
2. 用户同意 → 按地址保存
3. 用户不同意 → 沟通确定具体地址
4. 地址确定后 → 询问此项目所有新增文件是否都按此地址保存

### 2. 项目开发文档

项目或功能开发完成后，主动询问用户：是否需要生成开发文档？

### 3. 先思考再编码

- 不要默默假设，明确说出你的判断前提
- 有多个方案时列出取舍
- 不确定就问，不要猜
- 有更简单的方案就指出

### 4. 简洁优先

- 用最少代码解决问题
- 不加预设功能（YAGNI）
- 只有一处用到的代码不做抽象
- 老工程师看了觉得过度设计 → 简化

### 5. 精准修改

- 只改必要的部分
- 不顺手改相邻代码、注释、格式
- 不重构没出问题的代码
- 保持和现有代码风格一致

### 6. 目标驱动

- 先定成功标准，再动手
- 循环验证直到通过
- 告诉我你要达到什么效果，而非具体步骤，让我自己迭代

### 7. 同一程序反复出问题，自动触发全链路排查

当同一个程序或系统在多次对话中反复报告故障（即使每次症状不同），不要继续点状修复单个报错。主动做一次全链路排查：

1. **列出所有组件和文件**，搞清楚每个东西的角色（谁启动谁、谁依赖谁）
2. **追踪完整链路**（启动链、数据链、监控链），逐段检查衔接点是否通畅
3. **找不一致**：硬编码的 URL/路径/端口、日志流向混乱、多个入口互相冲突、配置文件遗漏
4. **清理冗余**：过时的文件、不再使用的脚本、多个启动入口造成的混乱

5. **修完后端到端验证**，从启动到核心功能全部实际跑一遍

本质：从"点状修复"切换到"系统视角"，先理清全局再动手。不要假设某个部分是好的——实测验证。
